各コードの使い方

ターミナルを開き、以下のコマンドで移動

$ cd maeda_lab_tools/使いたいコードのフォルダ

raw_dataフォルダにデータを抜き出したいファイルを入れ、以下のコマンドを入力しエンターでコードを実行

$ python convert_なんちゃら.py

なんちゃらにはそれぞれのフォルダに入っているコードによってちがう。

処理が終わればconverted_dataフォルダにファイルが生成されている。
（raw_dataにデータが残ったままなので、使い終わったら消しておくこと）